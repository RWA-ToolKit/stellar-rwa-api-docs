export const metadata = {
  title: "API · Compliance",
  description: "Non-PII compliance summary for a tokenized asset.",
};

# Compliance

<ApiEndpoint method="GET" path="/v1/assets/:id/compliance" description="Aggregate compliance summary — counts only, no addresses." />

Returns an aggregate view of an asset's KYC allowlist. It deliberately exposes
**counts, not addresses** — no personally identifying information leaves the API.

**Path parameters**

| Name | Type | Notes |
|------|------|-------|
| `id` | number | Registry id |

**Response schema**

| Field | Type | Notes |
|-------|------|-------|
| `total_records` | number | Addresses ever added to the allowlist |
| `approved` | number | Currently passing `is_allowed` |
| `suspended` | number | |
| `rejected` | number | |
| `pending` | number | |
| `with_expiry` | number | Records with a non-zero `expires_at` |
| `jurisdictions` | array | `{ jurisdiction, count }` breakdown |

<CalloutBox variant="note" title="approved + suspended + rejected + pending ≠ total_records">
`total_records` counts every address ever added to the allowlist. The status
fields only count addresses whose KYC record was **successfully read this
indexing cycle** — a record that fails to read or fails to parse contributes
to `total_records` but to none of `approved`/`suspended`/`rejected`/`pending`.
Don't assume the status fields sum to `total_records`; a gap between them
means some records didn't read cleanly this cycle, not that they're
unaccounted for on-chain.
</CalloutBox>

**Example**

```bash
curl http://localhost:8080/v1/assets/1/compliance
```

```json
{
  "total_records": 1,
  "approved": 1,
  "suspended": 0,
  "rejected": 0,
  "pending": 0,
  "with_expiry": 0,
  "jurisdictions": [
    { "jurisdiction": "US", "count": 1 }
  ]
}
```

**Errors**

Returns `404` with `{ "error": "not_found", … }` when the asset id is unknown.

<CalloutBox variant="compliance" title="Why counts, not addresses">
An asset's holder set can itself be sensitive. This endpoint gives issuers and
analysts the shape of an allowlist — how many are approved, where they're based,
how many approvals carry expiries — without publishing who they are. The
per-address KYC records live only on-chain and in the issuer's own tooling.
</CalloutBox>

The `jurisdictions` breakdown reflects the `jurisdiction` field of each KYC record.
Combined with the contract's `is_jurisdiction_blocked`, it helps issuers audit
their geographic exposure. See the [Compliance Guide](/docs/compliance-guide#jurisdiction-restrictions).
